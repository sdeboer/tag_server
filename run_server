#!/bin/sh

# Potential ENV vars, defaults are set in tag_server.app.src
# SESSION_COOKIE (ts_session)
# PORT (10100)
# CHASE_DOMAIN (localhost:4567)

erl \
	-pa ebin \
	-pa deps/*/ebin \
	-config app \
	-s tag_server
